{
  "name": "reactorfront-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build && npm run validate:structured-data",
    "capture:findy-score": "node scripts/capture-findy-score.mjs",
    "check": "astro check",
    "preview": "astro preview",
    "validate:structured-data": "node scripts/validate-structured-data.mjs",
    "validate:findy-score": "node scripts/capture-findy-score.mjs --validate"
  },
  "dependencies": {
    "astro": "7.2.1"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.10",
    "playwright": "^1.62.1",
    "typescript": "6.0.3"
  },
  "engines": {
    "node": ">=22.12.0",
    "npm": ">=9.6.5"
  }
}
