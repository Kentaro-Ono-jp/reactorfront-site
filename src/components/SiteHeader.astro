---
import BrandMark from "./BrandMark.astro";

const path = Astro.url.pathname;
type NavItem = {
  href: string;
  label: string;
  coffee?: boolean;
};

const nav: NavItem[] = [
  { href: "/", label: "ホーム" },
  { href: "/portfolio/", label: "ポートフォリオ" },
  { href: "/infrastructure/", label: "インフラ入門", coffee: true },
  { href: "/profile/", label: "プロフィール" },
];

const isActive = (href: string) =>
  href === "/" ? path === href : path.startsWith(href);
---

<header class="site-header" data-site-header>
  <div class="site-header__inner shell">
    <a class="site-header__brand" href="/" aria-label="ReactorFront ホーム">
      <BrandMark />
    </a>

    <button
      class="menu-button"
      type="button"
      aria-expanded="false"
      aria-controls="global-navigation"
      data-menu-button
    >
      <span class="sr-only">メニューを開く</span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </button>

    <nav class="site-nav" id="global-navigation" aria-label="グローバルナビゲーション" data-menu>
      <ul>
        {
          nav.map((item) => (
            <li>
              <a
                href={item.href}
                aria-current={isActive(item.href) ? "page" : undefined}
              >
                {
                  item.coffee && (
                    <span class="nav-coffee" aria-hidden="true">
                      <span class="nav-coffee__steam nav-coffee__steam--one"></span>
                      <span class="nav-coffee__steam nav-coffee__steam--two"></span>
                      <span class="nav-coffee__cup">☕</span>
                    </span>
                  )
                }
                <span>{item.label}</span>
              </a>
            </li>
          ))
        }
      </ul>
      <a class="button button--small button--dark site-nav__contact" href="mailto:kentaro.ono@reactorfront.jp">
        相談する
        <span aria-hidden="true">↗</span>
      </a>
    </nav>
  </div>
</header>

<script>
  const header = document.querySelector<HTMLElement>("[data-site-header]");
  const button = document.querySelector<HTMLButtonElement>("[data-menu-button]");
  const menu = document.querySelector<HTMLElement>("[data-menu]");

  const closeMenu = () => {
    button?.setAttribute("aria-expanded", "false");
    button?.querySelector(".sr-only")?.replaceChildren("メニューを開く");
    header?.classList.remove("is-menu-open");
  };

  button?.addEventListener("click", () => {
    const open = button.getAttribute("aria-expanded") !== "true";
    button.setAttribute("aria-expanded", String(open));
    button.querySelector(".sr-only")?.replaceChildren(open ? "メニューを閉じる" : "メニューを開く");
    header?.classList.toggle("is-menu-open", open);
  });

  menu?.querySelectorAll("a").forEach((link) => link.addEventListener("click", closeMenu));
  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") closeMenu();
  });
</script>
